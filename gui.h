#ifndef GUI_H
#define GUI_H

#include <iostream>
#include <vector>
#include "Grid.h"

class GUI{
private:
    Grid* grid;
    sf::RenderWindow window;
    vector<sf::Color> palete;
public:
    GUI(Grid* grid) //construteur

    void draw()

};

#endif